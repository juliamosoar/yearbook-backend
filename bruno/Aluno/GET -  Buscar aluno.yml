info:
  name: GET - Buscar aluno --- /alunos/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}alunos/6"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
