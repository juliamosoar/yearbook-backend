info:
  name: DELETE - Deletar aluno inexistente --- /alunos/:id
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}alunos/12"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 204", function() {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
