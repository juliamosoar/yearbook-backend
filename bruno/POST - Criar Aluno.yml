info:
  name: POST - Criar Aluno --- /alunos
  type: http
  seq: 2

http:
  method: POST
  url: https://sturdy-couscous-jjwgrrv567rw2p457-3000.app.github.dev/alunos
  body:
    type: json
    data: |-
      {
        "nome": "João Teste",
        "email": "joaomoreira@email.com",
        "senhaHash": "hash_temporario_456",
        "cidade": "Salinas"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 201", function() {
          expect(res.getStatus()).to.equal(201);
        });

        test("não deve retornar senhaHash", function() {
          expect(res.getBody()).to.not.have.property('senhaHash');
        });

        test("deve retornar o nome do aluno criado", function() {
          expect(res.getBody().nome).to.equal('João Teste');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
