info:
  name: GET - Buscar aluno inexistente --- /alunos/:id
  type: http
  seq: 3

http:
  method: GET
  url: https://fuzzy-goggles-q7gxvvjqrvjgfx95j-3000.app.github.dev/alunos/16
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 201 para aluno inexistente", function() {
          expect(res.getStatus()).to.equal(201);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
