info:
  name: DELETE - Deletar mensagem --- /mensagens/:id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}mensagens/15"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 204", function() {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
