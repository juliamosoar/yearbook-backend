info:
  name: PUT -  Atualizar aluno ---/alunos/:id
  type: http
  seq: 5

http:
  method: PUT
  url: https://fuzzy-goggles-q7gxvvjqrvjgfx95j-3000.app.github.dev/alunos
  auth: inherit
  body:
    type: json
    data: |-
      {
        "cidade": "Montes Claros"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("não deve retornar senhaHash", function() {
          expect(res.getBody()).to.not.have.property('senhaHash');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
