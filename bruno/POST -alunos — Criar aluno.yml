info:
  name: POST /alunos — Criar aluno
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/alunos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
